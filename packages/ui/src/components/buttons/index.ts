import { styled, Button } from 'tamagui';

export const generic = styled(Button, {
  fontFamily: '$button',
  backgroundColor: '$primary',
  color: '$white',
  padding: '$2 $4',
  borderRadius: '$md',
  cursor: 'pointer',
});
